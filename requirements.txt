# CognitiveOC v1.0.0 — derived dependency list
# No requirements.txt exists anywhere in the verified repo (only a nested one
# under corpus/corpus_wharehouse/ scoped to the corpus pipeline alone). This
# file was generated by scanning every import statement — including the
# lazy, function-level imports used throughout the codebase — across the
# whole tree. Treat it as a strong starting point, not a guarantee: pin
# versions once you have a working environment, via `pip freeze`.

# Core ML / inference
torch
transformers
sentencepiece
bitsandbytes
optimum
onnxruntime
sentence_transformers
numpy

# Retrieval / knowledge
datasketch
pyarrow

# Desktop + document/data handling
PySide6
Pillow
python-docx
openpyxl
pypdf
pytesseract      # also requires the Tesseract OCR system binary, installed separately
langdetect
openai-whisper

# Corpus / general
datasets
requests
PyYAML
psutil

# Dev / quality gates (already enforced in .github/workflows/ci.yml)
pytest
ruff
mypy
